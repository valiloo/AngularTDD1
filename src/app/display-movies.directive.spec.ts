import { DisplayMoviesDirective } from './display-movies.directive';

describe('DisplayMoviesDirective', () => {
  it('should create an instance', () => {
    const directive = new DisplayMoviesDirective();
    expect(directive).toBeTruthy();
  });
});
